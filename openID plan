Login button in both sign in and signup pages

Sign in function extracts the following info:
Full name
Email

Check if the user exists in database.

If exists: sign in

If user doesnt exist. Redirect to signupwGoogle page.

Page is split to two parts. User and venue. Toggleable by switch.
Fields for remaining data we need
    user: Phone num, passport
    venue: business name, phone num, address details

one signup button. Javascript code will decide, depending on switch, what accountt type it is.










Sign in:
Assign session details.